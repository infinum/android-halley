package com.infinum.halley.core.serializers.hal.models

/**
 * Base interface for models based on HAL JSON specification.
 * Each class model needs to inherit this interface in order
 * for serializers to work properly.
 */
public interface HalResource
