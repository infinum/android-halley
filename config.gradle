ext {
    def major = 0
    def minor = 0
    def patch = 1

    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 33,
            "targetSdk" : 33,
            "buildTools": "33.0.0"
    ]
    releaseConfig = [
            "group"      : "com.infinum.halley",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}